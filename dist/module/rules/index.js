"use strict";

var inflector = require("./inflector");
var filter = require("./filter");
var sort = require("./sort");

module.exports = {
    inflector: inflector,
    filter: filter,
    sort: sort
};