import gendersTest from "./genders";
import inflectionCasesTest from "./inflectionCases";
import functionTest from "./function";

export default function(shevchenko) {
    gendersTest(shevchenko);
    inflectionCasesTest(shevchenko);
    functionTest(shevchenko);
}
